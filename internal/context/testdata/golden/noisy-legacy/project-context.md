---
type: project-context
schema: struktly/project-context/v1
title: "Project Context: noisy-legacy"
description: "Deterministic repository context generated by struktly scan."
timestamp: $TIMESTAMP
---

# Struktly Project Context

Generated from repository files and Git metadata.

## Repository

- Repository name: noisy-legacy
- Repository root: `.`

## Top-Level Directories

- `_legacy`
- `archive`
- `docs`
- `legacy`
- `testdata`

## Detected Languages / Frameworks

- Go

## Build / Test Commands

- `go test ./...`
- `make test`
- `go build ./...`
- `go vet ./...`

## Existing Docs

- `README.md`
- `docs/current.md`

## Sensitive / Ignored Paths

- Default ignored directories: `.git`, `node_modules`, `vendor`, `.venv`, `venv`, `dist`, `build`, `.next`, `.turbo`, `.cache`, `__pycache__`, `.pytest_cache`, `.yarn`, `coverage`, `site`, `tmp`, `logs`, `.terraform`, `.direnv`
- Default ignored generated Struktly paths: `.struktly/runs`, `.struktly/memory/candidates`, `.struktly/context-packets`, `.struktly/scans`, `.struktly/ui`, `.claude/projects`, `.claude/sessions`, `.codex/sessions`, `.continue/sessions`, `.aws`, `.azure`, `.config/gcloud`
- `.env` / `.env.*`
- `*.pem`, `*.key`, `*.p12`, `*.pfx`
- `id_rsa`, `id_ed25519`, `*secret*`, `*credential*`, `*token*`
- Outside Git, root-level exact, directory, and glob .gitignore patterns are applied; negation and full Git semantics require a Git repository.

## Deprioritized Paths

These directories look stale or fixture-only (names like `legacy`, `archive`, `deprecated`, `old`, `testdata`) and were skipped when collecting docs, commands, and languages:

- `_legacy`
- `archive`
- `legacy`
- `testdata`

## Source References

- `Makefile`
- `README.md`
- `go.mod`
- `docs/current.md`
