{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "struktly/error/v1",
  "title": "Struktly CLI error",
  "description": "A structured command error for programs invoking the Struktly CLI.",
  "type": "object",
  "properties": {
    "schema": { "const": "struktly/error/v1" },
    "error": { "$ref": "#/$defs/error" }
  },
  "required": ["schema", "error"],
  "additionalProperties": true,
  "$defs": {
    "error": {
      "type": "object",
      "properties": {
        "code": { "type": "string" },
        "message": { "type": "string" }
      },
      "required": ["code", "message"],
      "additionalProperties": true
    }
  }
}
