{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "struktly/capabilities/v1",
  "title": "Struktly CLI capabilities",
  "description": "Versioned machine interfaces and additive features supported by one Struktly binary.",
  "type": "object",
  "properties": {
    "schema": { "const": "struktly/capabilities/v1" },
    "build": { "$ref": "#/$defs/build" },
    "commands": { "$ref": "#/$defs/strings", "description": "Stable machine commands, including context and tasks." },
    "schemas": { "$ref": "#/$defs/strings", "description": "Schemas emitted by the current binary. Input schemas this CLI only reads are published under schemas/ but not listed here." },
    "features": { "$ref": "#/$defs/strings", "description": "Additive feature identifiers, including tasks.partial_results." }
  },
  "required": ["schema", "build", "commands", "schemas", "features"],
  "additionalProperties": true,
  "$defs": {
    "strings": {
      "type": "array",
      "items": { "type": "string" }
    },
    "build": {
      "type": "object",
      "properties": {
        "version": { "type": "string" },
        "revision": { "type": "string" },
        "date": { "type": "string" }
      },
      "required": ["version"],
      "additionalProperties": true
    }
  }
}
