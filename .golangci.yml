version: "2"

linters:
  default: standard
  enable:
    - errorlint
    - misspell
    - unconvert
    - usetesting

formatters:
  enable:
    - gofumpt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
